from utils import employer_parser, vacancy_parser, write_to_table







#write_to_table(company_list, 'company')



write_to_table(vacancy_list, 'vacancy')

#with open('List.json', 'w', encoding='UTF-8') as file:
#    json.dump(dt, file, indent=2, ensure_ascii=False)
